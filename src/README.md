I ran the .ipynb in modal. To run notebooks there you need a deployed image, so first deploy the image at 
```modal_notebook_custom_image.py ``` using ```uv run modal deploy modal_notebook_custom_image.py```.
